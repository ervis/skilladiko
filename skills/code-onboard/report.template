# {{concept}}

## Spine
{{central abstraction}} — one sentence naming it.

{{producer}} `file:line` → {{abstraction}} `file:line` → {{consumer}} `file:line` → {{effect}} `file:line`

(One hop per line. `file:line` at each. Trigger at the start, side-effect at the end.)

## Neighborhood
k=1 collaborators off the spine, with file locations.

- {{sibling}} `file:line` — one-line role
- {{config}} `file:line` — one-line role
- {{adjacent station}} `file:line` — one-line role

## Trapdoors
Silently-doesn't-work traps. Hedge language ("appears to", "likely") if unconfirmed.

- {{trap}} `file:line` — what the obvious assumption is, why it fails
- {{trap}} `file:line` (likely) — the inference, why it's unconfirmed

## Invariants & constraints
What must hold for the concept to function. `file:line` for each.

- **Precondition:** {{what producer guarantees before hand-off}} `file:line`
- **Postcondition:** {{what consumer assumes on receipt}} `file:line`
- **Happens-before:** {{X must complete before Y}} `file:line`
- **Capacity/size limit:** {{limit}} `file:line`
- **Allowed values:** {{values}} `file:line`
- **Sanitization/filtering:** {{constraint}} `file:line`

(Include only the categories that apply; drop the rest.)

## Paths for your goal
{{user's stated task}} connected to findings.

- **Edit point:** {{which spine station}} `file:line`
- **Constraining neighbors:** {{what neighbors bound the change}} `file:line`
- **Idiomatic path:** {{path to achieve the goal}}
- Or: **Not supported** — {{why}} `file:line`; closest thing: {{alternative}} `file:line`

## Mirror tests
Tests exercising the spine. The user's safety net.

- {{test file}} `file:line` — what it exercises

## Misconceptions
≤3 trapdoors that break the mental model.

- {{misconception}} `file:line` — the wrong belief, the disproof

## Go deeper
1-2 directions, then a question.

- {{direction}}
- {{direction}}

Which (if any) to pursue?